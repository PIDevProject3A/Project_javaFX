package org.example;

import org.example.utils.MainFx;

/**
 * Redirige vers le point d’entrée réel {@link MainFx}. Toute la logique de lancement et la démo JDBC
 * optionnelle se trouvent dans {@code MainFx}.
 */
public class Main {

    public static void main(String[] args) {
        MainFx.main(args);
    }
}
